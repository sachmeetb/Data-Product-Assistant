steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -t
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: 'us-central1-docker.pkg.dev/eogwapq-agbg-internal-data-mig/bfsi-silver/backend:v1.1'
options:
  logging: CLOUD_LOGGING_ONLY
