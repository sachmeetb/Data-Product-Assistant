"""Synthetic bronze generation. Produces deliberately raw data -- inconsistent
casing, nulls, string-typed numerics, duplicates, late arrivals -- so the DQ
and quarantine paths are actually exercised."""
