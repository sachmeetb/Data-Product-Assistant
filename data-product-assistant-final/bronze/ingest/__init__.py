"""register -> profile -> land -> quarantine -> register into catalog.

The final step is the point: it closes the loop so discovery returns real
bronze matches instead of reading a static fixture."""
